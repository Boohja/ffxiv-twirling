// import type { PageServerLoad } from './$types';

// type JobOption = {
//   id: string,
//   name: string
// }

// export const load = (({ params }) => {
//   const jobs: JobOption[] = []
//   jobs.push({ id: 'ast', name: 'Astrologian' })
//   return { jobs };
// }) satisfies PageServerLoad;